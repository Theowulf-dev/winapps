1. Add font Unaego
2. Add App icon
3. Add App banner on welcome screen.
4. Unify the style across all windows.
5. Rename DistroSelection to DistroDetection

Steps>
Step 0: Welcome Screen
Step 1: Compatibility Check
Step 2: Dependency Install
Step 3: Create config files (User should be seeing the input fields and on hover there should be explanation.)
        After user has done filling config files they should be created at. If they exist already they should be overwritten.
        Create compose.yaml at ~/.config/winapps/compose.yaml
        Create winapps.conf at ~/.config/winapps/winapps.conf
        
Step : Select Installation backend `docker`, `podman`, `libvirt`
Step : Install Windows
Step : Configure Windows (Create Windows app that will do things automatically once inside windows.)
